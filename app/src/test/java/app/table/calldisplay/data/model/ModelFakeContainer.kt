package app.table.calldisplay.data.model

import app.table.calldisplay.ui.home.model.RequestOrder

internal object ModelFakeContainer {
    internal val requestOderFake = RequestOrder(100, 10, listOf())
}
